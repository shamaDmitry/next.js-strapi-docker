import { SigninForm } from "@/components/forms/sign-in-form";
import React, { FC } from "react";

const Page: FC = () => {
  return (
    <div>
      <SigninForm />
    </div>
  );
};

export default Page;
